/**
 * Memory configuration — simplified shim.
 * The new architecture no longer needs embedding providers or hybrid search weights.
 */

export { DEFAULT_MEMORY_CONFIG } from './types'
export type { MemoryConfig } from './types'
